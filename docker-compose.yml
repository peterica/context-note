services:
  context-note:
    build:
      context: ./dev
      dockerfile: Dockerfile
    container_name: context-note
    ports:
      - "${PORT:-8080}:3000"
    volumes:
      - ${WORKSPACE_ROOT:-/Users/seodong-eok/peterica}:/workspace
    environment:
      - WORKSPACE_ROOT=/workspace
    restart: unless-stopped
